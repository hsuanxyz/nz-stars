import { RepoSearchPipe } from './repo-search.pipe';

describe('RepoSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new RepoSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
